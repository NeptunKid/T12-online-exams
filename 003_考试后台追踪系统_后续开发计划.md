# 003 在线考试后台追踪系统（钉钉飞书接入版）：后续开发计划

更新时间：2026-08-05
适用项目：`$HOME/Documents/Codex/003_考试后台追踪系统_钉钉飞书接入版`

## 一、现状判断

本项目以 `002_考试后台追踪系统_钉钉登录版` 的可运行原型为只读来源基线。基线采用 Node.js 原生 HTTP 服务、静态 HTML/JS、单份 `public/exam_data.js` 题库、`data/submissions.json` 答卷文件、钉钉 OAuth 登录和阅卷白名单，已支持多人多次提交、客观题自动评分、问答题阅卷、补考权限和考生个人成绩查询。当前 `003` 目录仅含规划文档，尚未复制源码或生产答卷。

目前的关键边界是：考试/题库是单例；会话保存在进程内存；答卷使用 JSON 文件；阅卷权限来自环境变量；没有飞书登录、用户/角色管理、题库编辑、考试编排、通知队列、导入导出或 GitHub/飞书文档自动化。因此应先做数据与身份基础，再扩展业务功能。

## 二、目标架构

1. **身份与权限层**：抽象 `IdentityProvider`，同时接入飞书 OAuth 和钉钉 OAuth；统一用户表，以 provider + open/union id 建立身份映射；使用 RBAC（考生、阅卷人、考试管理员、系统管理员），不再依赖单一 unionId 环境变量。
2. **考试领域层**：`exams`（考试）、`question_banks`（题库）、`questions`（题目）、`exam_questions`（试卷组成/顺序/分值）、`exam_assignments`（可参加人员或组织）、`submissions`（答卷）和 `submission_question_snapshots`（交卷时题目快照）。历史答卷永远读取快照，后续编辑题库不会改变历史成绩。
3. **持久化层**：先将 JSON 导入 SQLite/PostgreSQL；所有写操作使用事务、审计字段和迁移脚本，保留原 JSON 备份及回滚方案。
4. **通知层**：统一 `NotificationService`，适配飞书应用消息/群机器人与钉钉工作通知/机器人；发送入队、重试、去重、失败记录和管理员告警。
5. **管理端**：考试、题库、用户权限、答卷、通知记录、审计日志集中在管理员后台；考生端继续保留工作台和成绩历史。
6. **公网与客户端层**：通过正式域名和 HTTPS 对公网提供服务；优先部署在云端，也可由本地专用设备通过 Cloudflare Tunnel 安全接入；考生和管理员无需处于特定网络，手机及电脑浏览器均可正常使用。

## 三、分阶段实施

### Phase 0：工程与安全基线

- 确认 GitHub 仓库地址、组织权限、默认分支和部署环境；在当前项目初始化 Git（目前项目目录未发现 `.git`），配置 `main`、`develop` 和功能分支规则。
- 建立 `.env.example`、密钥轮换方式、生产日志脱敏、备份策略和 CI 基础检查；禁止提交 `.env`、Cloudflare 凭证和答卷隐私数据。
- 加入 API 错误处理、请求日志、健康检查、基础测试框架；保留旧 JSON 原始副本。
- 确认公网域名、HTTPS、云端或本地设备部署拓扑；本地设备采用出站 Tunnel，不直接暴露 Node 或数据库端口。

验收：新环境可按文档启动；`main` 只接受合并请求；语法检查、单元测试和敏感信息扫描在 GitHub Actions 通过；手机蜂窝网络和电脑外网均能通过 HTTPS 打开测试入口。

### Phase 1：数据模型与多考试骨架（最高优先级）

- 经用户确认后，从 `002` 的备份副本设计并执行数据库迁移；为旧记录补充 `examId`、`questionSnapshot`、`provider` 等兼容字段，禁止让 `003` 与 `002` 共用生产数据文件。
- 将 `examData` 单例改为考试目录 + 题库集合；新增考试状态（草稿、发布、停止）、版本号、时长、总分、通过分数、答题规则和考试分配范围。
- 提交时写入试卷快照，阅卷、成绩和补考全部按 `examId`/快照计算；旧 `examTitle` 记录保持可读。

验收：可以创建两份互不影响的考试；同一考生可按考试分别参加和补考；编辑题库后历史答卷成绩不变；迁移前后旧记录数量和关键分数一致。

### Phase 2：题库与试卷管理

- 管理端支持题库新建、编辑、复制、归档和搜索；题目支持单选、多选、判断、问答、图片、解析、标签、难度和默认分值。
- 支持表格模板导入（CSV/XLSX）：先校验、预览错误、再批量入库；导出同一模板便于维护。图片采用对象存储或受控资源目录，不把答案接口暴露给考生。
- 试卷编排支持手动选题、按题型/标签/数量随机抽题、题目顺序、每题分值、考试时间和通过线；发布后生成不可变试卷版本。

验收：一题可属于多个题库/考试；导入错误可定位到行列；发布后的试卷可复现；考生 API 永不返回答案和解析。

### Phase 3：飞书 + 钉钉统一登录与权限

- 接入飞书 OAuth（企业内部应用）并保留钉钉 OAuth；统一登录回调、state/nonce、会话续期和退出机制。
- 建立用户同步任务，保存姓名、头像、部门、工号、provider 身份；支持按部门/人员/群组分配考试。
- 管理端增加角色、管理员、阅卷人和考试授权配置；默认最小权限，所有权限变更记录审计日志。

验收：两个平台的有权限员工均可登录；同一员工可关联两个平台身份；无考试权限无法开始；管理员与普通考生接口严格隔离。

### Phase 4：通知、阅卷工作流与结果回执

- 交卷事务成功后生成“待阅卷”事件，通知指定阅卷人或阅卷组；支持飞书和钉钉至少一种主通道，另一通道可配置为备份。
- 阅卷完成事务成功后通知考生，包含考试名、成绩、通过状态和结果链接；不在消息中发送答案等敏感内容。
- 建立通知 outbox、幂等键、失败重试、人工重发和送达状态；阅卷列表支持按考试、状态、人员、时间筛选和批量分派。

验收：交卷只产生一条可追踪通知任务；重复回调不重复发消息；通道失败可重试并告警；成绩保存与结果通知状态可审计。

### Phase 5：GitHub 分支交付与飞书文档自动总结

- 分支约定：`main`（生产）、`develop`（集成）、`feature/<模块>-<短名>`、`fix/<问题>-<短名>`、`release/<版本>`；每个 Agent 只在自己的功能分支提交，禁止直接推送 `main`。
- GitHub Actions 在 push/PR/release 时执行语法检查、测试、迁移检查、构建和敏感信息扫描；合并后生成版本号、变更摘要和部署包。
- 每次合并或发布自动生成 Markdown 总结（需求、变更文件、数据库迁移、测试结果、风险、回滚方式），调用飞书文档 API 追加到指定在线文档；凭证放 GitHub Secrets，失败进入重试队列并通知管理员。
- 同步内容只包含技术摘要和统计，不写入 Client Secret、答卷答案、身份证明等敏感数据；保留提交 SHA、分支、发布人和文档链接。

验收：一次合并能在 GitHub 留下可追溯流水线记录，并在飞书文档追加一条完整总结；任何 Agent 的分支都可独立构建、测试和回滚。

## 四、建议的首个开发迭代

先做 Phase 0 + Phase 1 的最小闭环：建立 GitHub 仓库与 CI、备份现有 JSON、引入数据库迁移、完成 `examId`/题目快照和双考试 API，再把现有考生端与阅卷端切换到新接口。不要在单例模型上继续堆题库管理或通知功能，否则后续迁移会重复改动评分、补考和成绩历史逻辑。

## 五、开始开发前需确认的外部条件

- GitHub 仓库 URL、组织权限、是否允许 GitHub Actions，以及生产分支保护规则。
- 飞书企业自建应用的 App ID/Secret、重定向地址、用户读取/消息/文档权限、目标文档 ID。
- 钉钉应用的 OAuth、通讯录/工作通知权限，以及阅卷组或管理员的组织标识。
- 通知接收规则：按考试指定阅卷人、部门负责人还是固定群；飞书和钉钉是否同时发送。
- 数据库部署位置、备份保留周期、生产域名和 Cloudflare Tunnel 的运行方式。
- 移动端支持范围、预计并发人数、可接受停机时间，以及最终选择云端还是本地专用设备部署。
