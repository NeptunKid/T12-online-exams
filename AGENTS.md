# 003 项目级 AGENTS.md

- 本项目用于开发同时接入钉钉与飞书的在线考核系统，目标目录是当前 `003_考试后台追踪系统_钉钉飞书接入版`。
- `001_考试后台追踪系统` 是稳定版，`002_考试后台追踪系统_钉钉登录版` 是来源基线；未经用户明确授权，不得修改这两个目录。
- 当前项目尚未复制源码或生产答卷。复制或迁移前必须说明来源、目标、备份和校验方案，并等待确认。
- `003` 必须使用独立数据文件或数据库，禁止与 `001`、`002` 共用 `data/submissions.json`。
- 开发前依次阅读本项目 `MEMORY.md`、两份计划书，以及 `002` 的 `AGENT_HANDOFF.md`；详细实施计划是正式执行依据，概要计划用于快速了解方向。
- 钉钉、飞书 OAuth 和消息凭证只能通过环境变量或部署 Secret 提供，禁止写入源码、文档或 Git。
- 公网服务必须使用 HTTPS；手机、电脑和非公司网络均须完成端到端验证。
- 每次开发只推进一个可验证、可回滚步骤；完成后记录修改文件、测试结果、风险、回滚方式和文档同步状态。
- 涉及操作时必须明确标注执行位置（本机终端、阿里云 Workbench、GitHub 页面或 Feishu），并说明执行后预期结果；不能只给出未标注环境的命令。
