# 考试后台追踪系统（钉钉飞书接入版）Memory

本文件只记录长期有效的架构决策、生产里程碑和未完成事项。逐次修复的细节与测试数字请查 `docs/development-summary/`，当前接手状态请查 `AGENT_HANDOFF.md`。

## 当前基线（2026-08-27）

- 核心业务已在生产验收：钉钉/飞书 OAuth、RBAC、同名账号人工合并、题库/试卷生命周期、整体组卷、题目与选项图片、答案规则、`.t12backup` 导入导出、考试授权、阅卷和钉钉/飞书通知。
- 生产已确认迁移 `0001` 至 `0012`；飞书通讯录同步成功，钉钉/飞书通知均实测成功。生产版本以用户最近确认的 `main` merge SHA 为准。
- 服务器采用 Cloudflare -> Caddy -> Node.js `127.0.0.1:3001` -> PostgreSQL `127.0.0.1:5432`；共享服务器唯一配置总账为 `/Users/neptun/Documents/Codex/aliyun-server-inventory.md`。
- 自动逻辑备份生产关闭（`T12_AUTO_BACKUP_ENABLED=false`），服务器最多保留一份完整 PostgreSQL dump；不得在容量、节流和异地恢复策略完成前重新启用。
- 外部对象存储目前只有提供商无关契约和内存模拟器，尚未连接 OSS/S3/R2、未创建 Bucket、未产生费用。
- PR #72、PR #73 已合并；本地分支可包含后续未部署提交，部署前必须重新确认 `origin/main` 和用户提供的合并 SHA。

## 长期架构决策

- 003 与 001/002 完全隔离，不复制或共用答卷、数据库、资源和 Secret；历史答卷依赖提交时快照，后续题库编辑不得改变历史成绩。
- 题目只属于题库；试卷从绑定题库选题，分值属于试卷组卷关系。试卷组卷参数、题库、选题、顺序和分值通过一个事务整体保存。
- 删除优先软删除/归档；永久删除必须检查有效引用并先保留可验证备份。导入备份总是生成新对象并失败整体回滚。
- OAuth、通讯录和消息发送采用平台适配器；第三方错误保留安全错误码/短消息，支持分页、去重、多部门关系、空目录诊断和网络异常，不输出凭证或原始身份 ID。
- 通知采用 outbox：事件键幂等、数据库锁领取、超时恢复、指数退避、最大尝试和送达回执；待阅卷只发管理员，成绩发考生和有效管理员并包含考生姓名。

## 阶段里程碑

- 2026-08-05 至 08-10：建立 003 项目边界、PostgreSQL 多考试模型、Caddy 公网 HTTPS、迁移框架、题库导入、评分、管理员角色和双平台 OAuth。
- 2026-08-11 至 08-14：完成题库/试卷组卷、图片资源、答案规则、备份导入导出、试卷修订、题库生命周期、删除策略和移动端体验；迁移推进到 `0008`-`0010`。
- 2026-08-15 至 08-21：完成通知 outbox、Worker、回执、飞书/钉钉 transport、成绩通知收件人规则和组卷统一保存。
- 2026-08-22 至 08-23：完成考试授权、`0012_organization_directory`、钉钉/飞书通讯录同步、发布前草稿保存和可诊断错误；用户确认生产通讯录同步成功。
- 2026-08-24 至 08-27：建立共享服务器总账、SSH 加固、自动备份暂停与 IOPS 事故记录、通知监控、加载超时修复、外部存储契约，以及钉钉/飞书接入指南。

## 未完成优先级

1. 通知 Worker 监控告警的生产部署、管理员验收和定期积压对账。
2. 外部对象存储真实适配、私有权限、生命周期、容量监控和隔离恢复演练；先确认供应商、区域、预算。
3. 通讯录差异同步（离职/禁用、改名、多部门变化、历史授权保留和审计）。
4. 电脑、手机、蜂窝网络及公司外网络的定期回归。
5. 飞书总结同步纳入受控发布步骤。

## 固定协作规则

- 每轮先读交接、计划、开发手册和相关摘要；一次只推进一个可验证、可回滚步骤。
- 先复现，再最小改动，再回归测试和质量门：`npm test`、`npm run check:syntax`、`npm run check:secrets`、`git diff --check`。
- GitHub 优先用命令推送和 `gh pr create` 自动建 PR；网络卡顿停止重试，交给用户本机终端。
- 生产部署统一按 `docs/production-deployment-runbook.md`；数据库写入前先 `pg_dump -Fc`，记录路径、大小和 SHA-256。
- 用户负责阿里云 Workbench 生产部署与真实桌面/手机验收；服务器配置变更必须同步 `/Users/neptun/Documents/Codex/aliyun-server-inventory.md`。

## 重要历史记录

- 自动逻辑备份曾因服务重启后重复全量打包造成约 879MB 工件和云盘 IOPS 限流；已暂停并清理 portable 工件，根因不是通知 Worker。
- SSH 已验证为 `PermitRootLogin no`、`PubkeyAuthentication yes`、`PasswordAuthentication no`，管理方式为 `admin + sudo`。
- 服务器当前不使用 Nginx、Redis、OSS、Docker/Podman 作为 T12 运行依赖；Caddy 是公网反向代理。其他项目是否使用需以共享总账现场盘点为准。
