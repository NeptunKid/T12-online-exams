# 考试后台追踪系统（钉钉飞书接入版）Memory

2026-08-25 [Codex] 更新当前总进度：Phase 0-4 核心闭环已完成并在生产验收，包含双平台登录/RBAC/同名人工合并、题库与试卷生命周期和整体组卷保存、备份导入导出、飞书/钉钉通知、考试授权及组织通讯录同步。用户确认生产 `c80cb83` 的飞书通讯录同步成功，钉钉和飞书通知均已实测成功；本机 `npm test` 354 项通过，语法、敏感信息和 diff 检查通过。下一优先级是通知 Worker 监控告警、外部对象存储和恢复演练、通讯录差异同步、定期跨设备回归及飞书总结自动化。最新完整摘要见 `docs/development-summary/2026-08-25-project-progress.md`；服务器软件、迁移和 SSH 状态见 Codex 根目录唯一总账 `/Users/neptun/Documents/Codex/aliyun-server-inventory.md`。

2026-08-05 [Codex] 创建 `003_考试后台追踪系统_钉钉飞书接入版` 项目目录，用于在 `002` 钉钉登录版的基础上规划钉钉与飞书双平台接入、多考试、题库、通知、GitHub、飞书文档同步及公网跨设备访问；当前仅放入概要与详细计划，尚未复制源码或生产答卷。
2026-08-08 [Codex] 已确定移除 Cloudflare Tunnel，采用阿里云东京服务器公网 80/443 + Caddy + Node.js 127.0.0.1:3001 + PostgreSQL 127.0.0.1:5432；新增 `deploy/Caddyfile`、`deploy/t12-exams.service`、公网部署文档与 `/healthz`、`/readyz` 检查，提交于 `chore/direct-public-deployment` 的 `1873bd0`。本机质量门 26 项测试、语法检查和敏感信息扫描均通过；因本机到 `ssh.github.com:443` 被限制，分支尚未推送/创建 PR。
2026-08-08 [Codex] 针对服务器部署报错补充 `T12_ENV_FILE` 支持，迁移可读取 `/etc/t12-online-exams/t12-online-exams.env`；补充 `/opt` 目录和 Caddy 旧配置诊断说明，修复提交为 `3a968a6`、`d8cca72`，本机质量门仍为 26 项通过。
2026-08-08 [Codex] 公网验收完成：`/healthz`、`/readyz` 和 `https://exam.t12group.com/` 均通过，架构为 Cloudflare -> Caddy -> Node.js -> PostgreSQL。已新增 Phase 2 CSV 题库校验预览（`6fe6b68`）：只读解析、无数据库写入；来源核对得到萃取原理 43 题（无分数列、含填空与 9 张图片）、消防 32 题/100 分、IT 37 题/99 分（需先脱敏）和候选咖啡师招聘笔试 23 题/72 分；“5 个剩余考试”中仍有两份未确认。
2026-08-08 [Codex] 在 `feature/phase2-csv-preview` 提交 `27e1249`：新增 `fill` 填空题题型、CSV 校验、PostgreSQL `0003_fill_question_type` 迁移、自动判分（答案别名/去首尾空格/不区分大小写）、考生输入框与阅卷人工改分，质量门 34 项测试通过；用户已明确不纳入咖啡师招聘笔试，餐饮法规和咖啡基础各 3 份 PDF 为扫描试卷，待 OCR 与逐题复核。当前推送仍受本机 `ssh.github.com:443` 限制，未重复尝试。
2026-08-08 [Codex] 复核 PDF OCR：法规 18 页、咖啡 20 页均未生成有效文字；macOS Vision 对所有页面返回 `Failed to create CVPixelBuffer`，因此没有题目/选项/答案可供确认，未写入仓库或题库。
2026-08-09 [Codex] 在 `feature/phase2-xlsx-question-drafts` 提交 `d90fc7f`：从萃取原理、消防基础、IT 基础 Excel 生成 CSV 审阅稿；分值/时长分别为 101/50 分钟、100/30 分钟、剔除 3 道操作题后 86/30 分钟。消防和 IT CSV 预览通过；萃取第 18、19 题为图表图片选项，因尚未建立受控资源映射而暂不允许提交。CSV 模板扩展到 `option_j`，`npm run check` 34 项通过。
2026-08-09 [Codex] 修正萃取图表题号：Excel 表头占第 1 行，因此第 17 题使用 `17-A.png` 至 `17-D.png`，第 18 题使用 `18-A.png` 至 `18-E.png`。新增 `public/question-resources/` 受控资源目录及 SHA-256 清单，CSV 增加 `option_image_a` 至 `option_image_j`，PostgreSQL API 和考生页支持受控选项图片；新增事务、幂等的三份题库/考试导入脚本与 Workbench 部署文档。本机质量门 36 项通过；尚未写入生产数据库。
2026-08-09 [Codex] 在 `feature/admin-role-management` 实现 PostgreSQL 管理员角色管理：钉钉登录登记/关联用户，首位管理员由 `DINGTALK_GRADER_UNION_IDS` 引导，后台可检索已登录钉钉用户并授予/撤销 `grader + system_admin`，权限变更写入 `audit_logs`，禁止自我撤权和移除最后系统管理员；本机质量门 32 项通过，尚未部署生产。
2026-08-09 [Codex] 已解决 PR #23 与最新 `main` 的 `MEMORY.md`、`package.json` 冲突：保留双方全部进展记录，并合并 Phase 2 与管理员 repository 的语法检查项；合并后质量门 42 项通过。
2026-08-09 [Codex] 在 `feature/admin-question-editing` 实现管理员会话提前校验、题干 `【】` 下划线显示和 PostgreSQL 已有试题编辑；仅 `exam_admin/system_admin` 可修改题干、选项、参考答案和解析，事务保存时递增题目及引用考试版本并写入审计日志，历史答卷快照保持不变；交卷增加试卷版本校验以避免编辑期间静默错分，完整质量门 64 项测试通过。
2026-08-09 [Codex] 在 `fix/t12-page-branding` 将考生首页及浏览器标题统一为“T12学习考核中心”，副标题为“我的学习与考核”；管理员登录页和后台顶栏以“T12学习考核中心”为主标题、“管理员阅卷后台”为副标题，未涉及数据库或业务逻辑，完整质量门 66 项测试通过。
2026-08-09 [Codex] 在 `fix/question-bank-layout-placeholders` 将题库维护改为先选试卷再显示对应题目，选择题当前答案在选项区高亮且统一通过单个文本框编辑；题干仅将内容为空白的 `【】`、`「」`、`[]` 转换为带下划线格式的连续空白字符，含文字括号保持原样，完整质量门 70 项测试通过。
2026-08-09 [Codex] 在 `feature/coffee-basics-exam` 从本地《咖啡基础知识》XLSX 生成 100 题 CSV 审阅稿，配置 `exam-coffee-basics` 为 60 分钟、总分 100、通过分 85，并沿用发布后全员有效钉钉用户授权；源表第 39 行跳号选项已无损压缩，另有 1 道问答题缺少源图片并列为上线风险。阶段进度估算约 65%，完整质量门 72 项测试通过；尚未导入生产 PostgreSQL。
2026-08-10 [Codex] 用户提供咖啡题库图片 `8-1.jpeg`、`36-1.jpeg`；在 `fix/coffee-question-images` 增加受控资源、副本最长边 1600px、`0005_question_stem_images` 可回滚迁移，并让题库导入、考生试卷、阅卷页和答卷快照保存题干图片；完整质量门 74 项测试通过。生产服务器此前因未拉取 PR #31 使用旧脚本，`--only` 报错但数据库备份成功且未写入；下一步在 Workbench 拉取最新 main、执行迁移后再导入 coffee。
2026-08-10 [Codex] 生产部署再次暴露用户混用：admin 在 codexdeploy 所有的仓库运行 npm ci，因无法 unlink `node_modules/.package-lock.json` 报 EACCES；随后迁移仅列出 0001-0004，说明服务器仍未拉到含 0005 的 `70a5f35`。固定修复为恢复仓库所有权并统一以 codexdeploy 执行 Git/npm，迁移与服务操作再由 admin sudo 执行。
2026-08-10 [Codex] 在 `feature/legal-cleaning-question-repair` 完成移动端工作台顶部栏自适应、首次/补考/额外补考文案和 PostgreSQL 额外授权事务扣减；阅卷页默认保留交卷快照，并可逐题提示题库变更后选择采用当前版本重新阅卷，评分依据保存到 `scores_json.reviewReferences`。本机质量门 78 项测试、语法检查和敏感信息扫描全部通过；未修改数据库结构或历史答卷。
2026-08-10 [Codex] 清洁卫生题库修复准备完成：由两份原始 Excel 生成 36 题来源数据，新增 47 个受控图片资源及默认 dry-run 的 PostgreSQL 修复脚本。脚本仅补当前题库空字段、记录审计并递增题目/考试版本；拒绝题目数或历史标识不一致，不更新分值、题型、授权、`data/submissions.json` 或任何历史答卷快照。质量门 83 项测试、语法检查与敏感信息扫描全部通过；生产执行前必须 `pg_dump -Fc` 备份。
2026-08-10 [Codex] 用户明确要求减少流程卡顿：后续 GitHub 外部网络操作（`git push`、`gh pr create`、PR checks/merge）优先由用户在本机终端执行；Codex 只准备代码、提交与最短命令。不得因网络问题反复等待或重试；如需代理或连接异常，立即暂停并说明。每条服务器命令须明确执行位置（本机终端或阿里云 Workbench）。
2026-08-10 [Codex] 清洁卫生题库修复已在生产 PostgreSQL 执行 `--apply` 并通过 `/readyz`；《餐饮相关法律法规》53 题试卷已合并、导入并发布，配置为 40 分钟、总分 100、通过分 85，题型为 19 单选、19 多选、15 判断。
2026-08-10 [Codex] 在 `feature/feishu-oauth-login` 完成飞书 OAuth 登录最小闭环：新增钉钉/飞书 Provider 抽象、飞书登录与回调、`feishu` 身份登记、考生端双平台入口和 provider 绑定 state；不按姓名合并身份，不修改现有考试授权或数据库结构。完整质量门 92 项测试、语法检查和敏感信息扫描通过；跨平台身份绑定与飞书考试授权留待下一独立步骤。
2026-08-10 [Codex] 在 `feature/cross-platform-users-mobile-admin` 完成跨平台真实姓名身份：钉钉必要时从企业通讯录补取真实姓名，飞书要求真实姓名；仅唯一标准化姓名自动绑定，同名歧义拒绝。新增可回滚 `0006` 跨平台全员考试授权、默认 dry-run 的历史身份整理脚本，以及管理员后台待批阅默认筛选和手机端列表/详情互斥视图；历史答卷快照和成绩保持不变。完整质量门 107 项测试、语法检查、敏感信息扫描通过；浏览器临时移动预览受 URL 安全策略阻止，需部署后实机验收。
2026-08-10 [Codex] 用户部署跨平台版本后确认生产服务正常：`t12-exams.service` active，`/readyz` 返回 `{"status":"ready","database":"ok"}`；重启后的首次本地 curl 失败属于服务启动窗口，后续探测成功。
2026-08-10 [Codex] 在 `feature/manual-question-entry` 完成题库手动录题最小闭环：管理员可按试卷/题库分类，在已有题库中新增五种题型、2 至 10 个选项、答案、解析和默认分值；服务端事务写入题目与审计日志，新题不自动加入试卷，不影响历史答卷。飞书总结脚本同时支持 `T12_ENV_FILE`。无数据库迁移，完整质量门 112 项测试、语法和敏感信息扫描通过。
2026-08-10 [Codex] 项目按详细计划加权估算完成约 70%：Sprint 1、2 已完成，Sprint 3/4 已完成主要可用闭环，通知 Outbox、完整组卷/组织同步、自动发布、监控和回滚演练仍待开发。`fix/feishu-plain-text-sync` 将飞书同步改为纯文本，版本化幂等标识可追加修正版；完整质量门 114 项测试、语法和敏感信息扫描通过。
2026-08-10 [Codex] 新增项目交接文档 `AGENT_HANDOFF.md`，汇总当前 Git 提交、生产架构、已完成 Phase/Sprint、考试发布状态、质量门、服务器权限教训、未确认事项、部署/同步命令、回滚方法及后续计划；用户素材保持未跟踪，不纳入提交。
2026-08-11 [Codex] 在 `feature/exam-authoring-v2` 完成首个可回滚步骤：管理员总提交、待批阅、已批阅统计卡改为可点击筛选按钮，并新增只执行聚合 `SELECT` 的试卷/题库模型审计工具，用于统计每份考试的题库数、组卷分值合计、声明总分差异、题库默认分值差异和孤立题目；未修改数据库、生产数据或历史答卷。完整质量门 120 项测试、语法检查、敏感信息扫描和 `git diff --check` 通过，尚未部署或同步飞书。
2026-08-11 [Codex] 在 `feature/exam-authoring-v2` 完成飞书管理员登录与跨平台授权步骤：管理员页新增按 Provider 配置显示的钉钉/飞书双入口，飞书独立用户和双平台用户可出现在管理员授权列表，角色校验继续使用内部用户，返回前端的身份标识保持遮蔽；未执行同名用户归并或数据库迁移。完整质量门 121 项测试、语法检查、敏感信息扫描和 `git diff --check` 通过，尚未部署或完成真实飞书管理员端到端验收。
2026-08-11 [Codex] 在 `feature/exam-authoring-v2` 完成经人工核对的同名跨平台账号合并：停止基于姓名的自动绑定，管理员仅可查看“纯钉钉/历史钉钉账号 + 纯飞书账号”的唯一同名候选；存在多个候选或已绑定双平台账号时一律阻断。系统管理员确认后以 PostgreSQL 事务合并身份、角色、考试授权、补考权限和答卷归属，题目快照、作答与分数保持不变；原账号改为 `disabled`，原审计记录不改写，新的审计记录保存迁移统计。登录、交卷、阅卷和补考授权共享身份变更锁以避免并发写回禁用账号；候选 POST 限制同源 JSON。命令行整理脚本仅允许 dry-run，不再允许 `--apply`。完整质量门 129 项测试、语法检查、敏感信息扫描和 `git diff --check` 通过，尚未部署、未写生产数据库或同步飞书。
2026-08-11 [Codex] 在 `feature/exam-authoring-v2` 完成试卷分值所有权兼容层：新增可回滚 `0007`，保留 `questions.score` 旧列但仅设默认 0，题库 API、手动录题和题库 UI 停止读写/显示分值；旧 CSV 分值只用于初始化 `exam_questions.score`。试卷新增可空 `question_bank_id`，仅对单题库试卷自动回填；跨题库与空试卷保持 NULL。新增 `pass_rate` 并按现有通过分/总分回填，为后续改分同步总分和通过线提供稳定比例。未修改 `submissions`、`submission_questions`、历史快照或成绩；完整质量门 131 项测试、语法检查、敏感信息扫描和 `git diff --check` 通过。代码依赖先执行 0007 再重启，尚未部署、未写生产数据库或同步飞书。
2026-08-11 [Codex] 在 `feature/exam-authoring-v2` 完成管理员草稿组卷：试卷可绑定一个 active 题库，从中部分选题或全选 active 题、稳定排序，并设置单题或全部已选题的试卷分值。所有写入均在事务内锁定试卷，校验草稿状态与乐观锁版本，拒绝跨题库/归档题，重算 `total_score` 和 `pass_score`，递增版本并写审计。已发布/排期/暂停试卷只读，未修改 `submissions`、`submission_questions`或历史成绩。组卷仓储/API/UI 聚焦测试 28 项通过；内置浏览器连接本机页面时卡住已立即终止，真实 PostgreSQL 及手机视觉验收待后续完成。本步骤无新迁移，但部署前仍必须先备份并执行 `0007`。
2026-08-11 [Codex] 在 `feature/exam-authoring-v2` 完成题目图片附件上传：新增可回滚 `0008_question_resources`，将 JPEG/PNG/WebP 图片本体、MIME、大小和 SHA-256 保存到 PostgreSQL，单张上限 5MB，同内容自动去重；服务端校验 Base64、magic bytes 与声明 MIME，拒绝 SVG 和伪装格式。题库管理员可在新建/编辑题目时上传、预览和移除最多 5 张题干图片；上传要求同源 JSON，读取要求登录并以 `nosniff` 图片响应返回。题目仅保存受控 URL，保存时验证资源存在；新题仍不自动入卷，已交答卷、快照和成绩不变。完整质量门 174 项测试通过；未部署、未写生产数据库、未做真实浏览器上传验收。
2026-08-11 [Codex] 在 `feature/exam-authoring-v2` 完成试卷与题库可移植备份首步：管理员可导出自包含 `.t12backup` ZIP，并一键导入为全新题库/题目/草稿试卷。v1 包保存题型、答案解析、组卷顺序与分值、作答规则、考试分配、补考权限及所有图片原始正文；题库题目不携带分值。导入校验格式版本、关系、题序、分值、整体和逐图片 SHA-256、MIME/magic bytes/大小，所有写入在单一事务内失败回滚，绝不覆盖已有对象。上传限制同源单文件 200MB、图片正文合计 100MB，ZIP 拒绝路径穿越、CRC 篡改和压缩炸弹。完整质量门 205 项测试通过；无新迁移、未写生产数据库，定期自动备份与外部存储适配留待下一独立步骤。
2026-08-11 [Codex] 在 `feature/exam-authoring-v2` 完成定期自动备份：新增可回滚 `0009` 保存逐试卷/题库运行和工件，支持 PostgreSQL `bytea` 或受控服务器目录；默认关闭，通过环境变量配置 1-720 小时周期、启动延迟和每对象 1-30 份保留。调度使用 PostgreSQL advisory lock 防多实例重复，单对象失败继续其他对象；文件原子写入并拒绝路径穿越，数据库/下载均复核 SHA-256 与大小。管理员可查看公开状态、立即运行和下载历史工件，不暴露服务器目录。`0009` down 在存在工件时拒绝回滚。完整质量门 232 项测试通过；未执行真实迁移或启用自动备份，生产启用前仍必须 `pg_dump -Fc`，数据库内逻辑包不能替代灾难恢复备份。
2026-08-11 [Codex] 生产首次启用文件系统自动备份时，12 个对象均因 `EACCES` 失败：父目录 `/var/backups/t12-online-exams` 为 `postgres:postgres 0750`，导致服务账号 `codexdeploy` 无法穿越到其拥有的 `portable` 子目录。修正方案为父目录 `root:root 0711`，并隔离 `postgres:postgres 0700` 与 `codexdeploy:codexdeploy 0700` 子目录；不删除失败记录、不修改答卷或历史快照。
2026-08-12 [Codex] 文件系统自动备份在生产修正目录权限后已成功生成，管理员下载了《002 历史题库：清洁卫生入职培训考试》36MB 工件。本机只读校验确认 ZIP/CRC 正常，但静态资源 `cleaning-12-1`、`cleaning-23-d` 实际为 WebP 而清单声明 JPEG，导致旧包无法通过严格回导；修正清单、静态响应和导出魔数校验后，内存重建的 1 题库、37 题、47 图片完整通过回导校验。旧包受 payload SHA-256 保护不可原地修改，部署后需重新生成。
2026-08-12 [Codex] 完成试卷版本化编辑与题库分类收敛：可新增/复制草稿，已发布、排期、暂停、结束试卷可显式转为草稿新版本，编辑参数、题库、选题、顺序和分值后重新发布；每次事务写入递增版本并记录 `audit_logs`，不修改历史答卷或快照，修订期间考生暂时无法进入。题库维护分类只显示题库，不再按试卷筛题。无新迁移，完整质量门 242 项测试通过；真实 PostgreSQL 与管理员桌面/手机端验收待部署后执行。
2026-08-12 [Codex] 修复考生答题与管理员阅卷的题目图片重复显示：旧版兼容层改为按静态文件 SHA-256 去重，当前题库路径优先，不同图片及不同选项标签仍完整保留；题库资源映射同时去除重复 URL。无迁移、无数据库写入，不修改题库、答卷、快照或图片文件。
2026-08-12 [Codex] 用户已将 `feature/exam-authoring-v2` 合并至 `main` 的 `664bac7` 并部署生产；本次不执行迁移，重启后 `t12-exams` active，公网 `/healthz` 和 `/readyz` 通过。用户已手动验收考生答题、管理员阅卷、题库分类、试卷管理和飞书管理员入口，全部通过。部署前 PostgreSQL 备份为 `/var/backups/t12-online-exams/postgres/t12_exams-before-664bac7-20260812122839.dump`，SHA-256 `bed0a49184d1512e86bc6aaf766785533a5ea4d95d0537821e5748c7ef18dd65`。
2026-08-12 [Codex] 新导出的 `002 历史题库：清洁卫生入职培训考试-20260812.t12backup` 完成双重只读校验：37,541,311 bytes，文件 SHA-256 `a79a3cf361e92bdc4c72c1c889a13816f0fba1c7bbd8be0ca05f3ded26bf7084`，ZIP/CRC、payload SHA-256、1 题库/37 题/47 资源、逐图 MIME 与 magic bytes、引用关系全部通过。旧 `20260811` 包仍因 WebP 误标 JPEG 被严格回导拒绝，不应导入。
2026-08-12 [Codex] 在 `feature/question-bank-lifecycle` 完成题库新建、元数据编辑、全量复制、软归档和恢复；所有变更使用事务、`version` 乐观锁和审计前后快照。归档不修改题目、试卷、组卷关系、答卷或快照，但会阻止题目写入及全部新组卷写路径；历史读取和备份保留。新增可回滚 `0010_question_bank_versions`，必须先迁移再启动新代码。完整质量门 271 项测试通过；未提交、未部署、未写生产数据库，真实管理员浏览器验收待部署后由用户执行。
2026-08-13 [Codex] 完成题库与试卷编辑体验修复：题库维护改为单下拉并在下方展示详情；新题和既有题目的每个选项支持受控图片上传/替换/移除；单选、多选、判断改为勾选式参考答案，填空支持结构化多空答案与顺序开关并兼容旧数组；考生端按空渲染多输入并提交数组；已发布试卷进入本地编辑态，只有首次真实保存才事务内转草稿，取消无写入，旧 `/revision` 路由只读兼容；批量分值输入保存后保持。无新迁移、无生产写入、未执行浏览器验收；`npm test` 276 项、语法检查、敏感信息扫描和 `git diff --check` 全部通过。
2026-08-13 [Codex] 根据生产截图继续精简题库维护：删除重复的“分类”下拉和题库名称展示，题目列表只显示一次题干及题号/题型；单选、多选参考答案直接在选项编辑行勾选，判断题不再显示选项栏，仅保留“正确/错误”参考答案。判断题内部兼容格式和历史答卷不变，无迁移、无生产写入、未做浏览器控制验收；完整质量门 279 项通过。
2026-08-14 [Codex] 修复生产验收发现的新题上传图片会清空未保存题干/选项、并使后续上传控件持续禁用的问题：上传前同步新题表单到本地草稿，上传成功或失败均在清除 busy 状态后重绘；新增图片上传回归测试。无迁移、无生产写入，需提交后重新部署并由用户验收题干图片、选项图片和保存流程。
2026-08-14 [Codex] 新增 `docs/production-deployment-runbook.md` 作为合并后的生产部署唯一入口：明确 Workbench 执行位置、`codexdeploy` 的 Git/npm 权限、版本确认、每次 `pg_dump -Fc`、可重复迁移、重启、`readyz`/公网健康检查、验收与按风险区分的回滚原则。文档变更未操作生产。
2026-08-14 [Codex] 完成试卷/题库生命周期和学员工作台修复：换绑定题库在单事务中自动清空旧选题与分值并重算；题库和试卷提供可恢复软删除，保留题目、组卷关系、答卷及快照；已批阅且通过的考试从“待考核科目”移入“已通过”。无新迁移、无生产写入；完整质量门 289 项、语法和敏感信息检查通过。
2026-08-14 [Codex] 修复生命周期与考生提交体验：已归档且未被试卷引用的题库可由管理员确认后永久删除，仍被引用时保留归档状态并拒绝删除；新试卷、编辑后试卷和草稿统一使用“发布”按钮；考生开始新考试或提交失败时恢复“提交试卷”按钮。无新迁移、无生产写入；本轮测试与部署待完成。
2026-08-14 [Codex] 根据验收继续收紧试卷生命周期和组卷保存：已删除试卷从管理员试卷列表隐藏，移除后台恢复入口，恢复仅依赖 PostgreSQL 备份或数据库回滚；选题保存请求新增逐题分值映射，允许一次同时勾选题目和设置分值，未保存选题时分值操作只更新本地草稿。无新迁移、无生产写入；`npm run check` 通过，295 项测试通过，待提交、PR 和生产部署。
2026-08-15 [Codex] 完成通知 Outbox 第一阶段：交卷事务在提交前为有效阅卷管理员身份幂等写入 `submission.created`，阅卷事务在提交前为考生的钉钉/飞书身份幂等写入 `submission.graded`；复用既有 `notifications` 表，不发送外部消息、不保存答案或解析、无新迁移。新增 Outbox repository 和回归测试，`npm run check` 通过，300 项测试通过；尚未提交、推送或部署生产。
2026-08-17 [Codex] 通知 Outbox 第一阶段已合并并部署生产，生产提交为 `23e08a0`；`t12-exams` active，内网 `/readyz` 与公网 `/healthz` 通过。生产验收确认 `submission.created` 有 10 条 pending、`submission.graded` 有 2 条 pending，说明交卷和阅卷事务均成功入队；发送 worker 尚未启用。部署前 PostgreSQL 备份为 `/var/backups/t12-online-exams/postgres/t12_exams-before-23e08a0-20260817164318.dump`，大小 3.3M，SHA-256 `81dd16e75884bfb022843aee01319f1239457cd015e5c22c9fbde8fc8f5dc2c7`。
2026-08-17 [Codex] 在 `codex/notification-delivery-worker` 完成飞书通知发送与恢复闭环：新增可回滚 `0011` 保存回执，Worker 默认关闭，使用 `SKIP LOCKED` 多实例领取、超时恢复、指数退避和最大尝试；固定启用时间阻止 12 条历史 pending 突然补发。系统管理员可在脱敏通知列表查看状态，并对已启用通道的 failed/abandoned 任务人工重发且写审计。当前仅支持飞书，钉钉等待正式消息权限和 Agent/机器人配置。无生产写入、无真实消息调用；完整质量门 318 项通过，待推送、PR、备份、迁移和分阶段生产验收。
2026-08-18 [Codex] 飞书通知 Worker Phase A 已部署生产 `a179c83`，`0011` 迁移、服务健康和 Worker 关闭验收通过，原有 10 条 `submission.created` 与 2 条 `submission.graded` 仍为 pending 且未发送；部署前备份为 `/var/backups/t12-online-exams/postgres/t12_exams-before-a179c83-20260818223352.dump`，SHA-256 `def2fdf9c37419dab19f4a3d03d6d07d90abd89cc76bb1f8c1987d81eb610744`。同时修复通知弹窗内层 960px 撑开外层 720px 导致的横向滚动，改为外层跟随视口、内层 100% 宽度；无迁移、无生产写入，完整质量门 319 项测试通过，待提交、部署和用户手动视觉验收。
2026-08-20 [Codex] 调整成绩通知收件人：`submission.created` 仍只通知管理员；`submission.graded` 同时通知考生和所有有效管理员，消息新增考生姓名并按平台身份去重。无新迁移、不回填历史通知；`npm run check` 的319 项测试通过，待提交、PR 和生产验收。
2026-08-21 [Codex] 完成钉钉工作通知 transport：新创建的钉钉任务使用 unionId 解析 userid 后按消息应用 AgentId 发送，通道配置支持 `feishu,dingtalk`并强制校验 AppKey/AppSecret/AgentId；旧 pending 任务不回填、不自动改写。无数据库迁移，待 PR 和分阶段生产验收。
2026-08-20 [Codex] 完成试卷组卷统一保存：新增整体保存事务接口，一次提交试卷参数、题库、选题、排序和逐题分值；前端移除单题保存和独立保存选题，题目按单选、多选、填空、判断、问答分组并支持分类批量设分后继续手动修改。无数据库迁移、无生产写入；`npm test` 323 项、语法检查和 `git diff --check` 通过，待提交、PR、部署和用户验收。
2026-08-20 [Codex] 修复归档题库永久删除误判：题库引用统计及题库详情的试卷计数均排除已归档试卷，因此仅被已删除试卷引用的题库可永久删除；无数据库迁移、无生产写入，题库生命周期回归测试和语法检查通过，待提交、PR、部署和验收。
2026-08-21 [Codex] 修复归档题库永久删除的数据库外键错误：永久删除前在同一事务中解除已归档试卷的组卷题目关系和题库外键，再删除题目及题库；有效试卷仍阻止删除，历史答卷快照不改写。无数据库迁移、无生产写入，题库生命周期测试和语法检查通过，待提交、PR、部署和验收。
2026-08-22 [Codex] 完成考试授权管理：组卷页面支持用户、部门、全部有效用户和全部有效钉钉用户授权；新增/移除使用试卷版本乐观锁、单事务和审计，重复授权幂等更新，禁止移除最后一条授权；学员试卷列表、详情、交卷和工作台均支持部门授权。更新 `admin.js` 缓存版本为 `20260822-1`，新增无效用户回滚、版本冲突、重复授权和最后一条授权保护测试。本轮无数据库迁移、无生产写入，`npm test` 331 项通过；待提交、自动创建 PR、用户部署和真实账号验收。详见 `docs/development-summary/2026-08-22-exam-assignment-management.md`。
2026-08-22 [Codex] 修复组织目录与发布前草稿：新增可回滚 `0012_organization_directory`，支持钉钉/飞书部门树、人员分页和多部门关系同步；管理员部门授权改为已同步部门下拉，服务端事务拒绝任意自由文本部门，人员列表继续读取全部有效用户。发布试卷前若题目、排序或分值未保存，会先询问是否整体保存；取消不清空草稿，确认后使用最新版本发布。新增组织 API/仓储/同步适配器及测试，`admin.js` 缓存更新为 `20260822-2`；本轮无生产写入，`npm test` 343 项、语法检查、敏感信息检查和 `git diff --check` 通过。详见 `docs/development-summary/2026-08-22-organization-directory-and-publish-fix.md`，待提交、自动创建 PR、合并后备份迁移和真实通讯录权限验收。
2026-08-22 [Codex] 修复组织同步验收失败：钉钉目录同步改用登录一致的 `openId/userid` 作为 `provider_subject`，同时按 `unionId`、openId 和历史身份匹配，避免同步时违反身份唯一约束；平台错误改为可操作的权限/配置提示，管理员同步区域改为独立响应式面板，前端缓存更新为 `admin.js?v=20260822-3`。本轮无生产写入，`npm test` 347 项、语法检查、敏感信息检查和 `git diff --check` 通过。生产验收前仍需在钉钉/飞书开放平台开通通讯录读取权限。详见同一开发摘要。
2026-08-22 [Codex] 继续修复生产同步失败：钉钉 token 请求改用正确的 `appKey/appSecret` 字段，部门解析兼容 `result.dept_id_list` 等对象格式，人员优先使用 `userid/openId` 并保留 `unionId` 匹配。`npm test` 348 项、语法检查、敏感信息扫描和 `git diff --check` 通过；尚未部署本修复。部署前需确认迁移 `0012_organization_directory` 已在生产执行。
2026-08-22 [Codex] 修复飞书通讯录同步成功但显示 0/0：请求指定 `open_department_id`，部门解析兼容 `department_id/open_department_id/id`，人员读取与部门 ID 类型一致，空部门时尝试全量人员接口；若部门和人员均为空则明确报权限/可见范围错误，不再静默成功。新增回归测试，`npm test` 351 项、语法检查、敏感信息扫描和 `git diff --check` 通过；本修复尚未部署。
2026-08-22 [Codex] 修复组织目录仓储将部门名称 `Map` 误当数组读取的问题；同步人员现在按第一个部门外部 ID 写入 `users.department`，并保留多部门关系。新增回归断言，`npm test` 351 项、语法检查、敏感信息扫描和 `git diff --check` 通过。本补丁尚未部署；用户反馈的“请求失败”仍需生产日志确认具体原因。
2026-08-22 [Codex] 针对飞书权限已开通但仍显示“请求失败”，将通讯录远程异常改为返回受限的平台错误码和短消息，并脱敏 token/secret/完整 URL；新增网络异常与错误码回归测试。新增通用开发手册 `docs/agent-development-handbook.md`，并将错误可诊断、单步可回滚、生产备份、质量门和用户验收原则写入 `AGENTS.md`。`npm test` 352 项通过；尚未提交、PR 或部署。
2026-08-22 [Codex] 针对飞书权限已开通但仍显示“请求失败”，将通讯录远程异常改为返回受限的平台错误码和短消息，并脱敏 token/secret/完整 URL；飞书部门 ID 类型先用 `open_department_id`，被租户拒绝时回退 `department_id`。新增网络异常、错误码和回退测试。新增通用开发手册 `docs/agent-development-handbook.md`，并将错误可诊断、单步可回滚、生产备份、质量门和用户验收原则写入 `AGENTS.md`。`npm test` 353 项通过；尚未提交、PR 或部署。
2026-08-22 [Codex] 继续修复生产仍显示“请求失败”：管理员前端先读取响应文本，非 JSON/空响应显示 `请求失败（HTTP 状态码）`；组织同步 handler 记录脱敏错误摘要；`admin.js` 缓存更新为 `20260822-4`。`npm test` 354 项、语法检查、敏感信息扫描和 `git diff --check` 通过；尚未提交、PR 或部署。
2026-08-23 [Codex] 生产日志确认飞书同步实际返回空部门/空人员，后端主动报 502；修复空部门时人员接口省略 `department_id` 导致静默空结果的问题，现显式查询根部门 `department_id=0`，并修复根范围重复用户解析。新增回归测试；尚未提交、PR 或部署。
2026-08-23 [Codex] 用户确认最新生产版本 `c80cb83` 的飞书通讯录同步成功。新增截至 2026-08-23 的总进度文档 `docs/development-summary/2026-08-23-project-progress.md`：核心业务闭环已完成，剩余重点为通知 Worker 监控告警、外部备份存储与恢复演练、通讯录差异同步和定期移动端回归。本机质量门仍为 354 项通过；本次仅更新文档，待同步飞书云文档。
