# 003 在线考试后台追踪系统（钉钉飞书接入版）：详细实施计划

更新时间：2026-08-05  
适用项目：`$HOME/Documents/Codex/003_考试后台追踪系统_钉钉飞书接入版`

## 0. 计划目标与实施原则

### 0.1 最终交付目标

系统最终应满足以下闭环：

1. 有权限的员工可以通过飞书或钉钉登录，管理员可以进入独立后台；
2. 管理员可以创建考试、维护题库、手动出题或表格导入、组卷、发布和停用考试；
3. 题目独立保存在题库中，可被多个考试复用；考试发布和考生交卷时保存版本快照；
4. 交卷后自动通知阅卷人，阅卷完成后自动通知考生；通知失败可重试、查询和人工补发；
5. 所有代码通过 GitHub 分支和 GitHub Actions 管理，每个 Agent 的工作均可追溯；
6. 每次合并或发布自动生成开发总结，并追加到指定的飞书在线文档。
7. 系统通过公网 HTTPS 访问，员工无需处于公司特定网络或使用特定设备；手机和电脑浏览器均可完成登录、考试、交卷和查看结果。

### 0.2 不可破坏的现有约束

- `001_考试后台追踪系统` 是独立稳定版，`002_考试后台追踪系统_钉钉登录版` 是本项目的只读来源基线；本计划只修改 `003_考试后台追踪系统_钉钉飞书接入版`。
- 当前 `003` 目录仅含规划文档，尚未复制源码或答卷。后续迁移前须经用户确认，从 `002` 复制源码和答卷备份；禁止删除、覆盖原数据，禁止让两个版本共用 `data/submissions.json`。
- 历史答卷必须能继续查看，历史成绩不能因题库编辑、重新组卷或通过线修改而变化。
- 钉钉、飞书 Client Secret、Cloudflare 凭证、员工身份数据和答卷内容不能进入 Git 提交或自动总结。
- 先完成一个可以回滚的最小闭环，再逐步扩展，不在单例模型上继续堆叠大量功能。
- 公网服务必须使用 HTTPS、最小开放端口和可撤销的访问凭证；不能把开发服务器端口直接暴露到公网。
- 本地设备部署必须考虑设备断电、睡眠、IP 变化、网络中断和远程更新；不得把“家用设备在线”当作高可用保证。

### 0.3 推荐技术决策

- 生产数据库推荐 PostgreSQL；本地和 CI 使用临时 PostgreSQL 容器或 SQLite 兼容测试层。若当前服务器无法部署 PostgreSQL，第一版可使用 SQLite WAL，但必须保留升级 PostgreSQL 的迁移脚本。
- 保留 Node.js 服务，但将当前单文件 `server.js` 拆分为认证、考试、题库、答卷、通知、存储和路由模块。
- 表格导入采用 CSV 为第一优先格式，XLSX 作为第二步；这样可以先完成稳定的校验和错误提示，再处理复杂的 Excel 格式。
- 通知采用 Outbox 模式：业务事务只写通知任务，后台 worker 负责调用飞书/钉钉 API，避免外部 API 故障阻塞交卷或阅卷保存。
- 公网部署优先采用云端 Linux 主机或托管平台；若使用本地设备，优先采用 Cloudflare Tunnel 等出站隧道，不开放路由器入站端口。
- 前端按移动优先设计，使用响应式布局、触摸友好的控件和稳定的计时/交卷状态；不依赖桌面端专属能力。

## 1. 开发前置条件与决策门

这些事项没有确认前，只能做本地数据模型和页面原型，不能上线真实登录或通知。

### 1.1 外部账号与环境确认

由项目负责人提供并记录在私密配置清单中：

- GitHub 仓库 URL、组织名、推送权限、是否允许 GitHub Actions、生产分支保护要求；
- 飞书企业自建应用：App ID、App Secret、OAuth 回调域名、用户读取权限、消息权限、文档编辑权限、目标文档 ID；
- 钉钉应用：Client ID、Client Secret、OAuth 回调地址、通讯录/工作通知权限；
- 通知接收规则：指定阅卷人、阅卷组、部门负责人或固定群；飞书和钉钉是否同时发送；
- 生产数据库位置、备份周期、Cloudflare Tunnel 运行方式、Node.js 进程守护方式；
- 人员身份字段规则：姓名、工号、部门、直属负责人、是否允许外包或离职人员登录。
- 公网域名、DNS 管理权限、TLS 证书方式、云端或本地设备部署选择、预计并发人数和可接受停机时间；
- 移动端支持范围（iOS Safari、Android Chrome、企业微信/飞书/钉钉内置浏览器）及是否允许使用内置浏览器完成 OAuth 回调。

验收：所有凭证都只存在本地 `.env`、部署 Secret 或 GitHub Secret；计划文档和 Git 历史中不出现真实值。

### 1.2 ADR 决策记录

在编码前建立 `docs/adr/`，至少记录：

- ADR-001：PostgreSQL 或 SQLite 的选择及迁移策略；
- ADR-002：飞书/钉钉身份合并规则；
- ADR-003：考试发布后的快照和题库版本策略；
- ADR-004：通知通道、重试次数和敏感信息范围；
- ADR-005：Git 分支、版本号和生产发布方式；
- ADR-006：公网部署拓扑、域名、HTTPS、Cloudflare Tunnel/反向代理和本地设备容灾策略。

每份 ADR 只允许一个当前结论，后续改变时追加新版本，不直接删除旧决策。

## 2. Phase 0：工程、安全和可回滚基线

目标：在不改变业务行为的情况下，让项目具备可测试、可提交、可回滚的基础。

### 2.1 初始化 GitHub 仓库与分支

执行步骤：

1. 在当前项目目录初始化 Git，并确认 `.gitignore` 覆盖 `.env`、Cloudflare 凭证、数据库文件、备份文件和本地日志；
2. 创建 `main`、`develop`；将现有可运行版本打上 `v0.1.0-dingtalk-json` 标签；
3. 建立分支命名：`feature/<模块>-<短名>`、`fix/<问题>-<短名>`、`chore/<任务>`、`release/<版本>`；
4. 每个 Agent 只能推送自己的分支，PR 必须包含变更摘要、测试结果、风险和回滚方式；
5. `main` 开启保护：禁止直接推送，至少一名审查者批准，CI 未通过不能合并。

验收：从全新目录 clone 后能启动旧版本；`git log` 能追溯到初始标签；GitHub PR 能显示 CI 状态。

回滚：Phase 0 不改业务数据，出现问题时回到 `v0.1.0-dingtalk-json` 即可。

### 2.2 建立配置和密钥边界

新增或整理：

- `.env.example`：只列变量名和空值；
- `docs/configuration.md`：说明本地、测试、生产配置来源；
- `scripts/check-secrets.js`：扫描常见 Token、Secret、私钥和真实员工数据；
- `docs/backup-restore.md`：说明答卷、数据库和上传资源的备份/恢复。

配置分组：

- `APP_*`：服务地址、环境、日志级别；
- `DB_*`：数据库连接和迁移模式；
- `DINGTALK_*`、`FEISHU_*`：身份和消息 API；
- `GITHUB_*`、`DOC_SYNC_*`：只供 CI 使用，不进入运行时客户端。

验收：前端接口不会返回 Secret；日志会对 unionId/open_id、手机号和 Token 脱敏；CI 对误提交 Secret 直接失败。

### 2.3 建立最低限度测试与质量门

先不要求完整端到端测试，先加入：

- `node --check`：服务端、考生端、后台脚本；
- 评分单元测试：单选、多选全对、漏选半分、错选零分、问答分数边界；
- 权限测试：未登录、普通考生、阅卷人、考试管理员、系统管理员；
- JSON 迁移测试：空文件、旧记录缺字段、损坏记录、重复 ID；
- API smoke test：健康检查、登录态、考试列表、交卷、阅卷保存。

验收：本地和 GitHub Actions 使用同一命令；任何测试失败都不能合并。

### 2.4 公网访问基线

在功能开发前先把公网访问方式定下来，避免后续 OAuth 回调、通知链接和移动端测试反复变更。

#### 方案 A：云端部署（推荐生产）

1. 准备一台云端 Linux 主机或托管 Node.js 服务，应用进程只监听 `127.0.0.1` 或私有网络端口；
2. 使用 Nginx、Caddy 或云负载均衡终止 HTTPS，再反向代理到 Node.js；
3. DNS 将 `exam.example.com` 指向云端入口，证书使用自动续期的 Let's Encrypt 或云证书；
4. 数据库放在同一私有网络或托管 PostgreSQL，不把数据库端口暴露给公网；
5. 使用 systemd、Docker Compose 或托管进程服务保证重启，配置健康检查和日志轮转；
6. 云安全组只开放 `443`，如需 HTTP 只用于重定向到 HTTPS；SSH 仅允许固定管理来源或使用云端会话工具。

#### 方案 B：本地设备部署（适合测试或低并发）

1. 本地设备运行 Node.js、数据库和 `cloudflared`；应用仍只监听本机回环地址；
2. 使用 Cloudflare Tunnel 等出站隧道绑定公网域名，不配置路由器端口映射；
3. 设置设备自动启动、自动重连、磁盘空间告警和进程守护；
4. 为设备准备 UPS、定时数据库备份和异地备份，明确设备离线时的人工处理方式；
5. 在公网访问文档中明确：本地设备断电或宽带中断会导致服务不可用，恢复后需检查通知队列和登录会话。

两种方案均必须使用正式 HTTPS 域名，不能把 `127.0.0.1`、局域网 IP、临时开发端口或带真实 Secret 的启动命令提供给使用者。

#### 公网基线验收

- 未登录访问首页、登录回调、退出登录和过期会话均正常；
- HTTP 请求被重定向或拒绝，Cookie 设置 `HttpOnly`、`SameSite`，生产环境启用 `Secure`；
- 公网只开放 443，数据库、管理 SSH、Node 内部端口不可直接访问；
- Cloudflare Tunnel/反向代理重启后服务可恢复，健康检查能区分“入口可用”和“应用可用”；
- 使用手机蜂窝网络、家庭宽带和公司网络各测试一次，不依赖同一局域网；
- 记录 DNS、证书、隧道、进程和数据库恢复步骤，并由另一名开发者按文档复现。

回滚：公网切换初期保留旧域名或旧服务入口；新入口异常时切回旧版本，禁止直接修改生产 DNS 记录而不留变更记录。

## 3. Phase 1：数据模型、迁移和多考试骨架

目标：先消除 `examData` 单例和 JSON 文件耦合，为后续题库、权限和通知提供稳定主键。

### 3.1 建议的数据表

最小表结构如下，具体 SQL 放在 `db/migrations/`：

| 表 | 关键字段 | 用途 |
| --- | --- | --- |
| `users` | `id`, `name`, `employee_no`, `department`, `status` | 统一员工主档 |
| `user_identities` | `user_id`, `provider`, `provider_subject`, `union_id/open_id` | 飞书/钉钉身份映射 |
| `roles` / `user_roles` | `role_code`, `user_id` | RBAC 权限 |
| `question_banks` | `id`, `name`, `description`, `status`, `owner_id` | 题库 |
| `questions` | `id`, `bank_id`, `type`, `stem`, `options_json`, `answer_json`, `score`, `version` | 题目及版本 |
| `exams` | `id`, `title`, `status`, `duration_seconds`, `pass_score`, `total_score`, `version` | 考试 |
| `exam_questions` | `exam_id`, `question_id`, `position`, `score`, `section` | 组卷关系 |
| `exam_assignments` | `exam_id`, `subject_type`, `subject_id`, `starts_at`, `ends_at` | 人员/部门/群组授权 |
| `submissions` | `id`, `exam_id`, `exam_version`, `user_id`, `status`, `scores_json` | 答卷主记录 |
| `submission_questions` | `submission_id`, `question_id`, `snapshot_json`, `answer_json`, `earned_score` | 交卷快照及逐题分数 |
| `retake_permissions` | `exam_id`, `user_id`, `remaining_count` | 补考权限 |
| `notifications` | `event_key`, `channel`, `recipient`, `status`, `attempts`, `last_error` | 通知 Outbox |
| `audit_logs` | `actor_id`, `action`, `resource_type`, `resource_id`, `before_json`, `after_json` | 操作审计 |

所有业务表均包含 `created_at`、`updated_at`；删除采用归档或软删除。答案、解析和员工身份字段必须限制在服务端或管理员接口。

### 3.2 JSON 迁移步骤

1. 经用户确认后，在 `002` 停止写入或进入维护模式，将其 `data/submissions.json` 复制为带时间戳的只读备份，再导入 `003` 的独立数据存储；
2. 编写 `scripts/import-submissions.js`，逐条验证 JSON、统计总记录、待阅卷数、已阅卷数和总分；
3. 为现有记录创建固定的默认 `examId` 和默认考试版本；将 `exam_data.js` 中的题目复制为 `questions` 与 `submission_questions.snapshot_json`；
4. 通过 `dingtalkUnionId` 创建 `user_identities`，无法识别的历史记录保留原姓名并标记为 `legacy_unmatched`；
5. 导入后执行数量、ID、分数、状态和答题字段对账；
6. 迁移脚本必须幂等，重复执行不能生成重复答卷；
7. 灰度运行期间保留旧 JSON 只读，不再让新接口写回旧文件。

验收：记录总数、每条答卷 ID、客观题分数、阅卷状态与迁移前一致；随机抽查不少于 10 条答卷的逐题结果。

回滚：停止新服务，恢复旧 Node 进程和原 JSON；数据库只读保留，不删除迁移结果，便于分析原因。

### 3.3 重构服务端边界

建议目录：

```text
src/
  app.js
  config.js
  http/router.js
  auth/{session,rbac,providers/}
  db/{client,migrations,repositories/}
  domain/{exams,questions,submissions,grading,notifications}/
  integrations/{dingtalk,feishu}
  workers/notification-worker.js
scripts/
tests/
```

先迁移纯函数和仓储，再迁移 API 路由；每一步保持现有 URL 可用，避免一次性重写整个 `server.js`。

新增核心接口：

- `GET /api/exams`：按当前用户权限返回可参加考试；
- `GET /api/exams/:examId`：返回去答案后的发布版本；
- `POST /api/exams/:examId/submissions`：按考试版本交卷；
- `GET /api/admin/exams`、`POST /api/admin/exams`、`PUT /api/admin/exams/:id`；
- `GET /api/admin/question-banks`、`POST /api/admin/question-banks`；
- 所有旧接口保留兼容层，并标记迁移截止版本。

## 4. Phase 2：题库、题目和表格导入

目标：让题目成为独立可复用资源，支持手工创建和可控的批量导入。

### 4.1 题库管理任务

1. 创建题库：名称、说明、负责人、状态；
2. 题目列表：按题型、标签、难度、状态、关键字筛选；
3. 题目编辑：题干、选项、标准答案、解析、分数、图片、标签、难度；
4. 题目版本：已被发布考试引用的版本不可直接覆盖，只能创建新版本；
5. 题目操作：复制、归档、恢复、批量归档；
6. 权限：题库负责人可编辑，考试管理员可引用，系统管理员可删除/恢复。

验收：同一题可以被多个考试引用；编辑题库不会改变已发布考试；归档题目不能进入新试卷但仍可查询历史。

### 4.2 CSV 导入模板

第一版固定列：

```text
external_id,type,stem,option_a,option_b,option_c,option_d,option_e,option_f,
answer,score,explanation,tags,difficulty,image_urls
```

校验规则：

- `type` 仅允许 `single`、`multi`、`judge`、`qa`；
- 单选/判断必须有一个答案，多选答案用 `A|B|C`；
- 选项字母必须连续且答案必须存在于选项中；
- `score` 必须是非负数字，问答题允许配置最大分；
- `external_id` 在同一题库内唯一；
- 图片 URL 必须使用允许域名或导入资源 ID；
- 空行跳过，错误行返回行号、列名、原因和修复建议。

导入流程：上传 -> 解析 -> 校验预览 -> 管理员确认 -> 事务写入 -> 生成导入报告。禁止“上传即入库”。

验收：错误 1 行不能阻塞错误提示；确认前数据库无变化；导入报告可以下载；同一文件重复导入能识别新增、更新和冲突。

### 4.3 XLSX 与图片

CSV 稳定后再增加 XLSX 适配器，复用同一内部行模型。图片不直接写入数据库大字段，保存资源 ID 和受控 URL；上传、鉴权、大小限制和病毒扫描作为单独任务。

## 5. Phase 3：考试编排、发布和考生端

目标：管理员可以配置一份可复现的正式试卷，考生只能看到授权考试和去答案内容。

### 5.1 考试生命周期

状态：`draft -> scheduled -> published -> paused -> closed -> archived`。

每次发布生成 `exam_version`，冻结：题目顺序、题目文本、选项、答案哈希、每题分数、考试时间、通过线、随机规则和允许次数。发布后只能复制为新版本，不能修改原版本。

### 5.2 组卷功能

- 手动选题：从题库筛选后加入试卷，支持拖拽排序；
- 按规则抽题：按题型、标签、难度和数量抽取；
- 随机策略：保存随机种子或保存具体题目清单，保证阅卷可复现；
- 分值校验：题目分值合计必须等于总分，问答题最大分必须明确；
- 考试授权：指定员工、部门、组织群组，支持开始/截止时间；
- 预览：管理员预览完整试卷，考生预览只隐藏答案和解析。

验收：发布前可预览并发现分值错误；发布后不能悄悄改变题目；同一考试版本的不同答卷使用同一题目快照。

### 5.3 考生端改造

将当前 `/api/exam` 改为考试列表和按 `examId` 进入；保留现有工作台、计时、提交、待阅卷和成绩详情。前端必须处理考试关闭、会话过期、重复提交、断网重试和交卷幂等。

### 5.4 跨设备与移动端适配

以手机竖屏为最低支持尺寸，再扩展到平板和电脑宽屏：

- 使用响应式断点和流式布局，不依赖固定宽度或悬浮在屏幕外的操作栏；
- 题目、选项、图片、计时器和提交按钮在 320px 以上宽度不横向溢出；
- 触摸目标至少约 44px，避免依赖 hover、右键、拖拽或桌面快捷键；
- 长题干、长选项、图片和问答输入框能自然换行，选择状态和未答状态清晰；
- 管理后台在手机上至少支持查看、筛选、阅卷和保存，不把关键操作限定在桌面端；
- 考试计时以服务端提交时间为准，浏览器切后台、锁屏、刷新或短暂断网后能恢复状态；
- 交卷按钮需要防重复点击，网络超时显示“提交状态未知”并提供安全重试，而不是直接再次创建答卷；
- OAuth 回调兼容系统浏览器以及公司允许的飞书/钉钉内置浏览器；不支持的内置浏览器要给出“使用系统浏览器打开”的明确入口。

兼容矩阵至少包含：iOS Safari 最近两个大版本、Android Chrome 最近两个大版本、macOS Safari/Chrome、Windows Chrome/Edge；每个组合验证登录、开始考试、图片加载、计时、交卷、成绩查看和退出登录。

验收：手机蜂窝网络下从登录到查看结果全流程完成；横竖屏切换不会丢失答题；桌面端和移动端的分数、状态和提交次数一致。

## 6. Phase 4：飞书/钉钉登录与 RBAC

目标：统一员工身份，但保留两个平台的登录入口和组织数据。

### 6.1 Provider 抽象

定义统一接口：

```text
getAuthorizationUrl(returnTo)
exchangeCode(code)
getCurrentUser(accessToken)
getOrganizations(accessToken)
```

实现 `DingTalkProvider` 和 `FeishuProvider`。每个回调均使用一次性 `state`、过期时间、白名单回跳地址和 HTTPS；生产会话使用持久化 Session Store，不再只存在 Node 内存。

### 6.2 身份合并规则

- 首次登录按 provider + provider_subject 建立身份；
- 同一员工两个平台身份不能仅凭姓名自动合并；
- 由系统管理员使用工号、企业邮箱或人工确认完成绑定；
- 离职/禁用员工不能新建考试，但历史答卷仍保留；
- 所有身份绑定、解绑和角色变化写入审计日志。

### 6.3 RBAC 与考试授权

最小角色：

- `student`：查看被分配考试、提交答卷、查看本人结果；
- `grader`：查看被分派答卷、阅卷、写阅卷批注；
- `exam_admin`：管理考试、题库、分配范围和阅卷组；
- `system_admin`：用户、角色、集成、审计和系统配置。

权限判断同时检查角色、考试范围和资源归属，不能只判断“是否管理员”。

验收：飞书和钉钉登录后都能进入同一员工工作台；普通考生访问管理 API 返回 403；没有考试分配的员工不能开始考试；切换身份不会读取他人答卷。

## 7. Phase 5：阅卷任务与消息通知

目标：把交卷、分派、阅卷、结果通知串成可追踪工作流。

### 7.1 事件和 Outbox

业务事务内写入事件：

- `submission.created`：答卷提交成功；
- `submission.assigned`：答卷分派给阅卷人；
- `submission.graded`：成绩和通过状态保存成功；
- `notification.retry_requested`：管理员手动重发。

Outbox 记录 `event_key`、事件类型、接收人、通道、模板版本、状态、重试次数、最后错误和送达回执。`event_key` 作为幂等键，确保同一事件不会重复发送。

### 7.2 通知模板

阅卷提醒至少包含：考试名、考生显示名、提交时间、答卷入口、截止时间和操作人要求。  
成绩通知至少包含：考试名、成绩、通过/未通过、通过线、结果入口；不发送标准答案和完整作答内容。

通道优先级由考试配置决定：飞书应用消息、钉钉工作通知、指定群机器人。失败时按指数退避重试，超过阈值进入人工处理列表。

### 7.3 阅卷后台改造

- 支持按考试、状态、阅卷人、部门、时间筛选；
- 支持分派和转派阅卷人；
- 保存成绩时使用事务，避免总分、通过状态和通知状态不一致；
- 支持草稿保存与最终提交，最终提交后记录阅卷人和时间；
- 提供通知状态、重发和错误详情，但不显示 Token。

验收：交卷成功后只生成一条待阅卷事件；阅卷保存失败不会发送“已完成”；重复点击保存或重放回调不会重复通知；消息失败能在后台重发。

## 8. Phase 6：GitHub CI/CD 与飞书文档自动总结

目标：把每个 Agent 的代码、测试、发布和总结串成可审计链路。

### 8.1 Commit 与 PR 规范

Commit 使用：`feat:`、`fix:`、`refactor:`、`test:`、`docs:`、`chore:`。PR 必须填写：

- 需求和范围；
- 变更文件和数据库迁移；
- 测试命令及结果；
- 数据兼容性；
- 风险和回滚步骤；
- 是否涉及第三方凭证或隐私数据。

### 8.2 GitHub Actions 流程

`pull_request`：安装依赖、语法检查、单元测试、API smoke test、迁移空库测试、Secret 扫描。  
`push develop`：构建测试镜像或部署包，执行集成测试。  
`release tag`：生成版本产物、迁移说明、变更日志和可回滚版本。

生产部署需人工批准；部署前自动备份数据库，部署后执行健康检查、关键 API 检查和错误率检查；失败自动停止继续发布并保留上一个版本。

### 8.3 自动总结并同步飞书文档

1. CI 从 PR 标题、Commit、变更文件、测试日志、迁移文件和发布标签生成 Markdown；
2. 过滤 `.env`、答卷字段、用户身份值和完整请求体；
3. 以“日期、版本、分支、PR、提交 SHA、需求、变更、测试、风险、回滚、部署结果”作为固定模板；
4. 使用 GitHub Secrets 中的飞书 App ID/Secret 获取文档访问 Token；
5. 通过飞书文档 API 追加到指定文档，保存 block/document 操作结果；
6. 同步失败不阻塞代码合并，但必须创建失败记录并通知系统管理员；
7. 每条总结包含 GitHub PR 链接和部署版本，方便从飞书追溯代码。

验收：合并一次 PR 后，GitHub 有完整流水线记录，飞书文档新增一条不含敏感信息的总结；同一 SHA 重试不会追加重复内容。

## 9. Phase 7：部署、监控和上线

### 9.0 公网部署实施步骤

#### 云端部署步骤

1. 创建独立的测试和生产运行环境，生产环境使用独立域名、数据库、对象存储和 Secret；
2. 配置 Node.js 运行时、进程守护、时区、日志目录和资源限制；
3. 配置私有数据库、数据库备份、迁移权限和只允许应用访问的网络规则；
4. 配置 Nginx/Caddy 或云负载均衡，启用 HTTPS、HSTS、请求体大小限制、超时和安全响应头；
5. 将 `exam.example.com` 指向入口，先用测试账号验证 OAuth 回调，再开放员工访问；
6. 配置监控：入口状态、应用健康、数据库连接、磁盘、内存、通知失败率和 5xx 错误率；
7. 发布后执行外网、手机蜂窝网络和非公司网络测试，确认没有依赖内网 DNS 或局域网 IP。

#### 本地设备部署步骤

1. 准备专用设备和固定工作目录，禁止使用个人临时目录或会自动休眠的笔记本作为长期生产设备；
2. 安装 Node.js、数据库和 Cloudflare Tunnel，分别创建非管理员运行用户；
3. 配置应用仅监听 `127.0.0.1`，由 Tunnel 将 HTTPS 公网请求转入本地服务；
4. 为 Node 服务和 Tunnel 配置自动启动、异常重启和网络恢复；
5. 配置每日数据库备份、异地备份和备份恢复演练；
6. 设置设备温度、磁盘空间、在线状态和 Tunnel 状态告警；
7. 明确设备离线应急流程：通知负责人、切换备用入口或云端临时服务、恢复后补发失败通知。

#### 公网安全检查

- 使用 `curl`、浏览器开发者工具和端口扫描确认仅暴露必要端口；
- 检查 HTTPS 证书有效期、HTTP 跳转、HSTS、Cookie 属性、CORS、CSRF 和 CSP；
- 检查登录回调 `state` 一次性使用、回跳地址不能跳转到外部恶意地址；
- 检查错误页面和日志不泄露文件路径、数据库连接、Access Token 或员工身份；
- 使用限流和请求体大小限制抵御重复提交、暴力请求和超大导入文件；
- 管理后台必须二次确认高风险操作，管理员账号使用最小权限。

### 9.1 测试环境

- 使用独立域名或端口、独立数据库和独立 OAuth 回调；
- 禁止测试环境读取生产 `submissions.json` 或生产数据库；
- 使用测试员工、测试题库和模拟消息通道；
- 完成双平台登录、考试、阅卷、通知和文档同步的端到端演练。
- 使用真实外网条件测试：手机 4G/5G、家庭宽带、公司网络和不同浏览器；至少模拟一次弱网、切后台、刷新和短时断网。

### 9.2 生产发布顺序

1. 冻结旧版本写入并完成数据库备份；
2. 执行迁移并对账，不通过则停止；
3. 部署新服务，保留旧版本可启动目录；
4. 先开放管理员和内部测试人员；
5. 验证登录、考试列表、提交、阅卷、通知、成绩查询；
6. 验证手机端、电脑端和非公司网络访问；
7. 再开放全体员工；
8. 发布后 24 小时观察错误率、通知失败率、数据库连接、公网可用率和响应时间。

### 9.3 回滚条件

满足任一条件即回滚：公网入口不可用、HTTPS/OAuth 回调失败、手机端无法完成交卷、历史答卷无法打开、分数对账不一致、普通用户越权、重复扣减补考次数、交卷成功但答卷丢失、通知队列持续失败、健康检查连续失败。

回滚动作：停止新版本、恢复上一版本服务、保持数据库备份和失败事件不删除、向管理员发送事故摘要；涉及数据库结构时执行反向迁移或切换到备份实例，禁止直接手工改生产表。

## 10. 推荐迭代顺序与完成定义

### Sprint 1：基线

GitHub、分支保护、配置模板、备份脚本、CI、评分和权限测试；确定公网域名、云端/本地设备拓扑，完成 HTTPS 和测试入口。

完成定义：旧功能行为不变，CI 通过，可从标签回滚；手机蜂窝网络和电脑外网均能打开测试入口；公网不暴露 Node 和数据库端口。

### Sprint 2：数据库与多考试

迁移脚本、考试/题目/答卷表、旧数据导入、题目快照、双考试 API。

完成定义：两份考试并行运行，历史分数对账通过。

### Sprint 3：题库与组卷

题库 CRUD、CSV 导入预览、手动组卷、发布版本和管理员页面。

完成定义：一题可复用，发布后快照不可变，考生看不到答案。

### Sprint 4：双平台身份与权限

飞书 OAuth、身份绑定、用户同步、RBAC、考试分配和管理员管理。

完成定义：两个平台均能完成权限隔离的登录和考试授权。

### Sprint 5：阅卷通知

阅卷分派、Outbox、飞书/钉钉消息、重试、重发和结果通知。

完成定义：交卷和阅卷结果均可端到端追踪，失败可恢复。

### Sprint 6：自动总结与生产发布

GitHub Actions 发布、飞书文档同步、备份、监控、公网 HTTPS、云端或本地设备部署、灰度和回滚演练。

完成定义：每次发布有版本、测试、总结、文档记录和可验证回滚点；使用手机和电脑、公司内外网络均能完成登录、考试、交卷和结果查看。

## 11. 每次开发任务的固定交付模板

每个 Agent 或开发分支完成任务时，必须提交以下内容：

```text
任务：
分支：
需求范围：
修改文件：
数据库迁移：无 / 有（说明）
兼容性影响：
测试命令与结果：
安全与隐私检查：
已知风险：
回滚方式：
公网入口/部署拓扑：
移动端与桌面端验证：
GitHub PR：
飞书文档同步状态：
```

只有“代码、测试、数据兼容性、回滚和总结”全部完成，才算一个开发步骤完成；仅能运行不代表可以上线。
