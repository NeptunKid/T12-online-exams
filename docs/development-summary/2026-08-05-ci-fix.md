# CI 修复总结

日期：2026-08-05  
基线：`origin/main`（已合并 Phase 0 基线）

## 问题

GitHub Actions 在 `actions/setup-node` 的 npm 缓存步骤失败，原因是仓库没有 `package-lock.json`。日志中的 Node 20 信息是弃用提示，不是失败根因。

## 修复

- 新增无依赖项目的 `package-lock.json`，使 `npm ci` 和 npm 缓存可用。
- 将 `actions/checkout`、`actions/setup-node` 更新到 `v5`。
- 将 CI 运行时 Node.js 更新为 24，消除 Node 20 运行时弃用配置。

## 验证

```text
npm ci --ignore-scripts --no-audit --no-fund：通过
npm run check：通过
6 项测试通过，敏感信息扫描通过
```

## 数据与凭证

无数据库迁移；未读取或修改 `.env` 值，未触碰历史答卷。

## 回滚

恢复 CI 文件到上一提交并移除 `package-lock.json` 即可；不涉及业务数据。
