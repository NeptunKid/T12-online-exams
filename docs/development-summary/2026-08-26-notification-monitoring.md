# 通知 Worker 监控（2026-08-26）

## 本次变更

- 新增只读 `notification-monitor` 纯函数，根据待发送、失败、已放弃和陈旧发送中任务生成告警。
- 通知配置增加积压、失败和已放弃阈值，默认分别为 25、0、0；发送中陈旧窗口沿用 `T12_NOTIFICATION_STALE_AFTER_SECONDS`。
- 通知列表统计查询带出各状态最早时间，管理员通知页显示“正常”或具体告警原因。
- 不改变通知入队、发送、重试、人工重发和数据库写入逻辑；不新增迁移，也不改变 `/readyz`。

## 验证

- 新增监控纯函数测试，覆盖正常、积压/失败/已放弃和陈旧发送中场景。
- 更新通知配置、仓储和管理员 UI 测试。
- 本轮质量门已通过：`npm test` 357 项、`npm run check:syntax`、`npm run check:secrets`、`git diff --check`。

## 风险与回滚

- 监控只读；阈值配置错误只会影响提示，不会改变通知发送结果。
- 回滚方式为回滚本次代码提交并重启服务；未执行生产数据库写入，无需数据库回滚。

## 交接

- 生产部署前无需新增迁移或新增 Secret。
- 如需调整阈值，仅在阿里云 Workbench 的环境文件中修改对应变量，然后按固定部署流程重启并由用户验收通知页。
