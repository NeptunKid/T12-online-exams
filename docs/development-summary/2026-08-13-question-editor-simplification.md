# 2026-08-13 题目编辑界面精简

## 完成内容

- 题库维护左侧只保留一个“当前题库”下拉框，删除重复的“分类”下拉框。
- 题库详情不再重复显示题库名称；题目列表不再重复显示题库名称，只显示题干、可选题号和题型。
- 单选题和多选题将参考答案选择合并到选项编辑行，分别使用单选和复选控件，不再在下方重复渲染选项内容。
- 判断题不再显示选项栏，只显示题干和“正确/错误”参考答案；服务端继续使用兼容值保存，不改变历史题目和答卷格式。
- 修正新上传图片型选项在文本为空时的保存校验。
- 更新管理员页面静态资源版本，避免部署后继续使用旧缓存。

## 验证

- `npm test`：279/279 通过。
- `npm run check:syntax`：通过。
- `npm run check:secrets`：通过。
- `git diff --check`：通过。
- 未执行浏览器控制验收、数据库迁移、生产数据库写入或 GitHub 网络操作。

## 数据与回滚

- 无数据库迁移，不修改 `submissions`、`submission_questions` 或历史快照。
- 判断题数据库兼容结构保持不变，仅隐藏管理员界面的选项栏。
- 回滚时恢复本轮代码提交并重启服务即可；生产部署前仍需创建新的 `pg_dump -Fc`。
