# 飞书纯文本总结同步

日期：2026-08-10

任务：修正飞书在线文档中的开发总结格式  
分支：`fix/feishu-plain-text-sync`

## 变更

- 同步前将 Markdown 总结转换为纯文本。
- 移除标题、列表、任务复选框、表格分隔符、粗体、斜体、删除线、代码围栏和链接语法。
- 保留正文、链接地址、表格字段和值以及代码内容。
- 使用 `plain-text-v1` 版本化同步标识，使已经同步过的 Markdown 版本可以追加纯文本修正版。
- 同步脚本继续支持通过 `T12_ENV_FILE` 读取生产环境配置。

## 安全与兼容性

- 不读取或输出任何真实凭证。
- GitHub 仓库中的 Markdown 总结不变，只改变发送到飞书的内容。
- 同一纯文本版本重复同步仍保持幂等，不会重复追加。
- 不涉及数据库迁移、考试数据或历史答卷。

## 验证

- `npm run check`：通过。
- `npm test`：114 项通过。
- `npm run check:secrets`：通过。

## 风险与回滚

- 旧的 Markdown 内容已经存在于飞书文档中时，脚本会追加纯文本修正版；旧内容不会被自动删除。
- 如需清理旧段落，请在飞书文档中人工删除旧的 Markdown 段落，避免误删其他总结。
- 代码回滚：部署上一份已验证提交并重启 `t12-exams`。

## 同步状态

- GitHub PR：待创建。
- 飞书文档：合并并部署后重新执行同步命令。
