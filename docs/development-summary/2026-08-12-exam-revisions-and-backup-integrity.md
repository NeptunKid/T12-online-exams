# 试卷版本化编辑与备份完整性修正

日期：2026-08-12

## 完成内容

- 新增试卷、复制试卷、已发布试卷进入草稿修订及重新发布。
- 草稿可修改名称、时长、通过比例、题库、选题、顺序和分值。
- 所有写入使用事务、乐观锁、版本递增和 `audit_logs`；不更新历史答卷或题目快照。
- 发布前校验至少一题、总分大于零、题库一致且题目仍为 active。
- 题库维护分类只显示题库，不再显示或接受试卷筛选。
- 静态题目图片的响应和备份导出使用真实图片 MIME，并扫描全量资源清单与文件魔数。

## 生产边界

- 已发布试卷进入修订后状态变为 `draft`，重新发布前考生暂时不能进入。
- 本步没有数据库迁移；部署代码前仍保留当前 PostgreSQL 完整备份。
- 自动备份目录权限修正后已在生产成功生成工件。
- 2026-08-11 下载的清洁卫生题库旧包包含两项错误 MIME，不能直接回导，也不能绕过 payload SHA-256 手工篡改。部署后重新生成并下载新包。

## 验证

- `npm run check`：242 项测试通过。
- 敏感信息扫描、语法检查和 `git diff --check` 通过。
- 旧包 ZIP/CRC 正常；只读修复两项 MIME 并重算摘要后，1 个题库、37 道题、47 张图片完整通过回导校验。
- 未连接真实 PostgreSQL，也未执行管理员桌面/手机端浏览器端到端验收。

## 回滚

- 回退本步应用代码和前端资源即可；无新 schema 需要回滚。
- 已经开始但尚未重新发布的试卷修订不能靠回退代码恢复状态，应由管理员核对审计后重新发布，或从操作前 PostgreSQL dump 恢复到隔离库确认。
