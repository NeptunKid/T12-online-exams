{
  "name": "exam-tracker",
  "version": "1.0.0",
  "private": true,
  "description": "在线考试提交追踪与后台阅卷系统",
  "scripts": {
    "start": "node server.js",
    "dev": "node server.js",
    "test": "node --test tests/*.test.js",
    "check:syntax": "node --check server.js && node --check public/exam.js && node --check public/admin.js && node --check scripts/migrate.js && node --check scripts/import-legacy-submissions.js && node --check scripts/verify-legacy-import.js",
    "check:secrets": "node scripts/check-secrets.js",
    "check": "npm run check:syntax && npm test && npm run check:secrets",
    "migrate": "node scripts/migrate.js",
    "migrate:plan": "node scripts/migrate.js --plan",
    "import:legacy": "node scripts/import-legacy-submissions.js",
    "verify:legacy-import": "node scripts/verify-legacy-import.js"
  },
  "engines": {
    "node": ">=18"
  }
}
